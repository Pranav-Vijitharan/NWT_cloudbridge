version: 2

models:
  - name: raw_products
    columns:
      - name: productID
        tests:
          - unique
          - not_null
      - name: categoryID
        tests:
          - not_null
      - name: supplierID
        tests: 
          - not_null
  - name: raw_category
    columns:
      - name: categoryID
        tests:
          - unique
          - not_null

